// Global feature flags for UI/debug surfaces.

// When true, the on-screen Status / Performance pane will not be shown.
// Flip to true to hide the perf overlay entirely.
export const HIDE_STATUS_PANE = false;
